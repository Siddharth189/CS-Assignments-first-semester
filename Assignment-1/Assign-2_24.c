#include<stdio.h>
#include<math.h>

int main ()
{
// 24. Write a program in C to ﬁnd whether a given number is odd or even.
  //If the number is odd, print O. Otherwise, print E.You cannot use any if () statement.

   int a=3 ;

   (a % 2 == 0)  ? printf("even") : printf("odd");

return 0;
}
/*
   (a%2)?odd:Even;
